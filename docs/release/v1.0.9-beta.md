# v1.0.9-beta

## Changes

- Made the archive hint arrow open the same archive candidate state on both PC and smartphone.
- Kept archive as an explicit two-step action: open candidate, then press `アーカイブ`.
- Added PC horizontal wheel support for opening the archive candidate.
- Fixed the web nested-button structure around memo rows.

## Verification

- `npm run typecheck`
- `npm test -- --runInBand`
- `npm run build:web`
- Playwright screenshots for desktop and mobile archive hint, archive toast, and horizontal wheel behavior.
