# v1.0.6-beta

## Changes

- Pin and archive buttons moved to memo cards (same row as copy button)
- Each card now shows: content | timestamp | pin (◆) | archive (↓) | copy (⧉)
- Removed floating action bar; notification button still appears when editing a memo with a detected date
